export const formatPhoneNo = (phone, countryIso) => {
    if (!phone) return '';
    return phone;
};

export const formatPhoneNoInternational = (phone, countryIso) => {
    if (!phone) return '';
    return phone;
};
