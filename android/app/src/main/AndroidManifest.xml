<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network access for webhook calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Lets us wait for a validated internet network before firing the webhook
         (the data path is briefly unavailable the instant a call rings). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Read phone state to detect incoming calls -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!--
      READ_CALL_LOG is required on Android 10+ (API 29+) to obtain the incoming
      caller number from TelephonyManager.EXTRA_INCOMING_NUMBER.
      Not needed on API 28 and below where READ_PHONE_STATE is sufficient.
    -->
    <uses-permission android:name="android.permission.READ_CALL_LOG"
        android:minSdkVersion="29" />

    <!-- Allow auto-start after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Required to run a foreground service (keeps process alive when app is killed) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ requires per-type permission for foreground services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Draw over other apps — lets us show an incoming-call banner on the lock screen
         and over third-party apps. User must grant this manually in system settings. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:description="@string/app_description"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:theme="@style/LaunchTheme"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!--
        Static BroadcastReceiver: always active, fires on PHONE_STATE changes.
        android:exported="true" is required so the OS can deliver the broadcast.
      -->
      <receiver
        android:name=".CallBroadcastReceiver"
        android:exported="true">
        <intent-filter>
          <action android:name="android.intent.action.PHONE_STATE" />
        </intent-filter>
      </receiver>

      <!-- Restarts call listening after device reboot -->
      <receiver
        android:name=".BootReceiver"
        android:exported="true">
        <intent-filter>
          <action android:name="android.intent.action.BOOT_COMPLETED" />
          <action android:name="android.intent.action.QUICKBOOT_POWERON" />
        </intent-filter>
      </receiver>

      <!--
        Persistent foreground service. Keeps the process alive after the app is
        swiped away so CallBroadcastReceiver can send webhooks without needing to
        cold-start the process.
      -->
      <service
        android:name=".CallListenerForegroundService"
        android:foregroundServiceType="dataSync|specialUse"
        android:exported="false">
        <property
            android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
            android:value="Monitors incoming phone calls and sends a webhook notification to the Foodhub restaurant platform to identify customer callers." />
      </service>

      <!-- Floating overlay shown over the incoming-call screen. Requires SYSTEM_ALERT_WINDOW. -->
      <service
        android:name=".CallOverlayService"
        android:exported="false" />

      <!--
        HeadlessJS service: starts a JS context in the background so that
        sendCallToWebhook (callSaga) can run even when the app is killed.
        foregroundServiceType="dataSync" is required on Android 10+.
      -->
      <service
        android:name=".CallDetectionTaskService"
        android:foregroundServiceType="dataSync|specialUse"
        android:exported="false">
        <property
            android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
            android:value="Background JS task that sends a webhook notification to Foodhub when an incoming call is detected while the app is not in the foreground." />
      </service>

    </application>
</manifest>
